/* import { Router } from "express";
import { createLike, deleteLike } from "../controllers/postLike.controllers";

export const postLikeRoute = Router()

postLikeRoute.post("/postCreate", createLike)
postLikeRoute.delete("/postDelete/:id", deleteLike) */